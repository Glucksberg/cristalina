{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "generate-schemas": "pnpm --filter @cristalina/types generate-schemas",
    "openclaw": "node packages/openclaw/dist/cli.js",
    "cli": "node packages/cli/dist/runner.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
